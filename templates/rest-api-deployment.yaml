apiVersion: apps/v1
kind: Deployment
metadata:
  name: rest-api-deployment
  labels:
    app: {{.Values.rest-api.name}}
spec:
  replicas: {{.Values.rest-api.replicas}}
  selector:
    matchLabels:
      app: {{.Values.rest-api.labels.app}}
  template:
    metadata:
      labels:
        app: {{.Values.rest-api.labels.app}}
    spec:
      containers:
        - name: {{.Values.rest-api.template.spec.containers.name}}
          image: {{.Values.rest-api.template.spec.containers.image}}
          ports:
            - containerPort: {{.Values.rest-api.template.spec.containers.ports.containerPort}}
          env:
            - name: DATABASE_URL
              valueFrom: 
                configMapKeyRef:
                  name: postgres-config
                  key: DATABASE_URL
            - name: FLASK_ENV
              value: development