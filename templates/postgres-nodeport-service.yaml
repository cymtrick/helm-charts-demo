apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
    - port: 5432
      nodePort: {{.Values.postgres.ports.nodePort}}
      targetPort: {{.Values.postgres.ports.targetPort}}
  selector:
    app: {{.Values.postgres.metadata.labels.app}}
